name: RDP Auto-Rotate Every Minute

on:
  workflow_dispatch:

jobs:
  setup-rdp:
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: windows-2019
            server: 1
          - os: windows-2019
            server: 2
          # ... أضف بقية السيرفرات هنا ...

    runs-on: ${{ matrix.os }}
    timeout-minutes: 3600

    steps:
      - name: Setup RDP and Rotator Bot
        shell: pwsh
        run: |
          # إعداد RDP كالمعتاد
          Set-ItemProperty -Path 'HKLM:\System\CurrentControlSet\Control\Terminal Server' -Name "fDenyTSConnections" -Value 0 -Force
          Set-ItemProperty -Path 'HKLM:\System\CurrentControlSet\Control\Terminal Server\WinStations\RDP-Tcp' -Name "UserAuthentication" -Value 0 -Force
          netsh advfirewall firewall delete rule name="RDP" 2>$null
          netsh advfirewall firewall add rule name="RDP" dir=in action=allow protocol=TCP localport=3389
          Restart-Service TermService -Force
          
          $password = ConvertTo-SecureString "admin@123" -AsPlainText -Force
          if (Get-LocalUser -Name "TOOLBOXLAP" -ErrorAction SilentlyContinue) {
              Remove-LocalUser -Name "TOOLBOXLAP" -Force
          }
          New-LocalUser -Name "TOOLBOXLAP" -Password $password -AccountNeverExpires -PasswordNeverExpires
          Add-LocalGroupMember -Group "Administrators" -Member "TOOLBOXLAP"
          Add-LocalGroupMember -Group "Remote Desktop Users" -Member "TOOLBOXLAP"
          
          $ip = (Invoke-WebRequest -Uri "https://api.ipify.org" -UseBasicParsing -TimeoutSec 10).Content.Trim()
          
          Write-Host "============================================"
          Write-Host "RDP ACTIVE - Server ${{ matrix.server }}"
          Write-Host "IP: $ip:3389 | User: TOOLBOXLAP | Pass: admin@123"
          Write-Host "This server will rotate to a new one every 60 seconds"
          Write-Host "============================================"

      - name: Download and Run Rotation Bot
        shell: pwsh
        run: |
          $desktop = [Environment]::GetFolderPath("Desktop")
          $botUrl = "http://31.57.97.230:5000/rotator.py"  # يجب أن يكون البوت الجديد موجوداً على هذا الرابط
          
          # تحميل البوت الجديد الذي يقوم بإعادة تشغيل RDP كل دقيقة
          for ($i = 1; $i -le 3; $i++) {
              try {
                  Invoke-WebRequest -Uri $botUrl -OutFile "$desktop\rotator.py" -UseBasicParsing -TimeoutSec 30
                  break
              } catch {
                  Start-Sleep -Seconds 5
              }
          }
          
          # تثبيت Python إذا لم يكن موجوداً
          $pythonPath = (Get-Command python -ErrorAction SilentlyContinue).Source
          if (-not $pythonPath) {
              $installer = "$env:TEMP\python.exe"
              Invoke-WebRequest -Uri "https://www.python.org/ftp/python/3.11.0/python-3.11.0-amd64.exe" -OutFile $installer
              Start-Process -FilePath $installer -ArgumentList "/quiet InstallAllUsers=1 PrependPath=1" -Wait
              Remove-Item $installer -Force
              Start-Sleep -Seconds 10
              $pythonPath = "C:\Python311\python.exe"
          }
          
          # تشغيل البوت (سيقوم بإعادة تشغيل RDP كل دقيقة)
          if (Test-Path "$desktop\rotator.py") {
              Start-Process -FilePath $pythonPath -ArgumentList "`"$desktop\rotator.py`"" -WindowStyle Normal
          }

      - name: Keep Alive for 6 Hours
        shell: pwsh
        run: |
          # الـ Workflow يبقى شغال 6 ساعات، والبوت هو المسؤول عن التغيير
          $ip = (Invoke-WebRequest -Uri "https://api.ipify.org" -UseBasicParsing -TimeoutSec 10).Content.Trim()
          $endTime = (Get-Date).AddHours(6)
          
          Write-Host "============================================"
          Write-Host "Server ${{ matrix.server }} - ROTATION MODE ACTIVE"
          Write-Host "The bot will change RDP settings every minute"
          Write-Host "Current RDP: $ip:3389"
          Write-Host "============================================"
          
          while ((Get-Date) -lt $endTime) {
              Start-Sleep -Seconds 60
              $remaining = [math]::Round(($endTime - (Get-Date)).TotalMinutes, 0)
              Write-Host "[$(Get-Date)] Server ${{ matrix.server }} - $remaining minutes remaining"
          }
